# Gallery Authentication microservice

Handles user credentials, performs **validation** and issues **Access/Refresh tokens**. Uses **Redis** for session management and **PostgreSQL** for persistence of encrypted user's passwords.